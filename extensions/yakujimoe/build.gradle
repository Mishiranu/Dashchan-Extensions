rootProject.configureExtension(project, [
		chanNameUpper: 'YakujiMoe',
		versionName: '1.1',
		apiVersion: 1,
		hosts: ['ii.yakuji.moe']
])

dependencies {
	implementation project(':engines:wakaba')
}
