rootProject.configureExtension(project, [
		versionName: '1.10',
		apiVersion: 1,
		icon: 'ic_storage',
		hosts: ['arhivach.org']
])

dependencies {
	implementation project(':libraries:templateparser')
}
